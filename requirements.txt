# PhotoStudioHub - All dependencies
# Install: pip install -r requirements.txt
# Models download automatically on first run (~1 GB total, then 100% offline)

# Core image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0,<2.0
scipy>=1.11.0

# RAW file support (CR2, NEF, ARW, DNG, etc.)
rawpy>=0.18.0

# Perceptual hashing for duplicate detection
imagehash>=4.3.1

# Face detection, face mesh, segmentation (local models, no API)
mediapipe>=0.10.0

# Expression, age, gender detection (local models, no API)
deepface>=0.0.79
tf-keras>=2.15.0

# Face recognition / people sorting across album
face-recognition>=1.3.0
dlib>=19.24.0

# Background removal (U2Net model, local)
rembg>=2.0.50

# Super-resolution upscaling (local model)
basicsr>=1.4.2
realesrgan>=0.3.0

# Inpainting - blemish, dust, object removal (local model)
lama-cleaner>=1.0.0

# Face alignment for consistent portrait series
face-alignment>=1.4.1

# Skin tone classification
skin-tone-classifier>=1.2.0

# Professional LUT color grading
colour-science>=0.4.3

# Chromatic aberration correction (lens profiles included)
lensfunpy>=1.2.0

# Object / scene detection (YOLOv8, local model)
ultralytics>=8.0.0

# Color palette extraction
Pylette>=0.1.5

# EXIF metadata read/write/strip
piexif>=1.1.3
exifread>=3.0.0

# XMP sidecar file writing
python-xmp-toolkit>=2.0.1

# Progress bars and CLI
tqdm>=4.65.0
click>=8.1.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0
